// Reusable home score variable //
let homeScore = document.getElementById("home-score");
let awayScore = document.getElementById("away-score");

// Reusable variable
let homeScoreCount = 0;
let awayScoreCount = 0;